import VenueForm from "@/app/ui/login-signup-component/venue/venueForm";

const page = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <VenueForm />
    </div>
  );
};

export default page;
